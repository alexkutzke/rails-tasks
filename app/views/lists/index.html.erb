<p id="notice"><%= notice %></p>

<%= t('welcome_html', username: current_user.email) %>

<h1><%= t List.model_name.human %> </h1>

<%= link_to 'New List', new_list_path %>

<br>

<% @lists.each do |list| %>
  <h2><%= list.name %> <%= link_to "[+]", new_list_task_path(list) %>
                       <%= link_to '[Edit]', edit_list_path(list) %>
                       <%= link_to '[X]', list, method: :delete, data: { confirm: 'Are you sure?' } %></h2>
  <% if list.tasks.empty? %>
    <%= "Nenhuma tarefa.".html_safe  %>
  <% else %>
    <table>
      <thead>
        <tr>
          <th>Task</th>
          <th colspan="2"></th>
        </tr>
      </thead>

      <tbody>
        <%= render list.tasks %>
      </tbody>
    </table>
  <% end %>
<% end %>
